version: '3.8'

services:
  password-checker:
    build:
      context: .
      dockerfile: Dockerfile
    image: password-strength-checker:latest
    container_name: password-strength-checker
    stdin_open: true
    tty: true
    environment:
      - JAVA_OPTS=-Xmx256m
